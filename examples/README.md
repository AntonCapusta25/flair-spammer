Copy example files to project root (skips existing files):

```bash
make setup
```

Or copy manually from `examples/` → `tokens.txt`, `config.json`, etc.

Then open `tokens.txt` and paste your token.
